[user]
	name = Michael Fisher
	email = {{ .email }}
[pull]
	ff = only
[push]
	default = simple
[alias]
	co = checkout
	br = branch
	ct = commit
	st = status
	aa = add --all :/
	pu = push
    pf = push --force
[credential]
	helper = cache
[filter "lfs"]
	required = true
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
[help]
	autocorrect = 1
{{- if (index . "github") }}
[github]
    token = {{ (onepasswordDetailsFields "hhkyn33w7zh7dhfea33dmczbxa" "ckgxbstxior66nkkp7lemennq4" "communitytechalliance").password.value | quote }}
{{- end }}
