#!/bin/bash

function plugin_install() {
	vim +PluginInstall +qall
}

function compile_ycm() {
    cd $HOME/.vim/bundle/YouCompleteMe && \
    python install.py --system-libclang --all
}

if [[ $(plugin_install) -eq 0 ]]; then
	if [[ $(compile_ycm) -eq 0 ]]; then
		exit 0;
    fi
fi

